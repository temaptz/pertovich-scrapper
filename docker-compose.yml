services:
  scraper:
    build:
      context: .
      dockerfile: Dockerfile
    env_file: .env
    environment:
      - HEADLESS=virtual
    volumes:
      - ./temp:/app/temp
    init: true
    shm_size: '2gb'
    restart: on-failure
